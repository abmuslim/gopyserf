apiVersion: v1
kind: Pod
metadata:
  name: BE-pod
  labels:
    qos: be
spec:
  schedulerName: custom
  containers:
  - name: busybox
    image: busybox:latest     
    command: ["/bin/sh", "-c", "while :; do yes > /dev/null; done"]
