FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive

# Install required packages including certs for curl HTTPS
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
    curl \
    ca-certificates \
    iproute2 \
    iputils-ping \
    net-tools \
    vim \
    sudo \
    kmod \
    systemd && \
    rm -rf /var/lib/apt/lists/*

# Install k3s
RUN curl -fsSL -o /usr/local/bin/k3s https://github.com/k3s-io/k3s/releases/download/v1.32.4+k3s1/k3s && \
    chmod +x /usr/local/bin/k3s

# Inject containerd config to enable cgroups properly
RUN mkdir -p /etc/rancher/k3s/agent/etc/containerd/ && \
    printf "[plugins.\"io.containerd.grpc.v1.cri\".containerd.runtimes.runc.options]\n  SystemdCgroup = true\n" \
    > /etc/rancher/k3s/agent/etc/containerd/config.toml.tmpl

# Copy Kubernetes manifests
COPY qos-controller-daemonset.yaml /tmp/qos-controller-daemonset.yaml
COPY deployment-scheduler.yaml /tmp/deployment-scheduler.yaml
COPY cluster-role.yaml /tmp/cluster-role.yaml
COPY cluster-role-binding.yaml /tmp/cluster-role-binding.yaml
COPY service-account.yaml /tmp/service-account.yaml
COPY test-pod.yaml /tmp/test-pod.yaml
COPY ram_price.yaml /tmp/ram_price.yaml
COPY storage_price.yaml /tmp/storage_price.yaml
COPY vcpu_price.yaml /tmp/vcpu_price.yaml
COPY vgpu_price.yaml /tmp/vgpu_price.yaml

CMD ["sleep", "infinity"]
 
